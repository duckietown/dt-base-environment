#!/usr/bin/env bash

PACKAGE=$1
VERSION=$2
DESTINATION="${3:-${SOURCE_DIR}/${PROJECT_NAME}/packages/}"

set -ex

mkdir -p "${DESTINATION}"

cd /tmp/
wget --no-check-certificate https://github.com/${PACKAGE}/archive/refs/tags/${VERSION}.zip
unzip ${VERSION}.zip -d "${DESTINATION}"
rm ${VERSION}.zip
