#!/usr/bin/env bash

PACKAGE=$1
VERSION=$2

set -ex

cd /tmp/
wget --no-check-certificate https://github.com/${PACKAGE}/archive/refs/tags/${VERSION}.zip
unzip ${VERSION}.zip -d "${SOURCE_DIR}/${PROJECT_NAME}/packages/"
rm ${VERSION}.zip
