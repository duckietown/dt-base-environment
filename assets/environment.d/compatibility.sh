# we set these environment variables for backward compatibility with code from DTProjects v3 and older

export DT_MAINTAINER="${DT_PROJECT_MAINTAINER}"
export DT_REPO_PATH="${DT_PROJECT_PATH}"
export DT_LAUNCH_PATH="${DT_PROJECT_LAUNCHERS_PATH}"
